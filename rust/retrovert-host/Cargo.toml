[package]
name = "retrovert-host"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Host-side Rust bindings for the Retrovert plugin ABI"

[dependencies]
libloading = "0.8"
thiserror = "2"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true

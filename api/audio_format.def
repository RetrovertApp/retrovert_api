#[module(retrovert)]

enum AudioStreamFormat {
    U8 = 1,
    S16 = 2,
    S24 = 3,
    S32 = 4,
    F32 = 5,
}

#[attributes(Copy)]
struct AudioFormat {
    audio_format: AudioStreamFormat,
    channel_count: u32,
    sample_rate: u32,
}
